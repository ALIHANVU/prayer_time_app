import 'package:flutter/material.dart';

class AppColors {
  // ─── Акцент ───
  static const Color accent = Color(0xFF4A6CF7);
  static const Color accentLight = Color(0xFF8B5CF6);

  // ─── Зоны намаза ───
  static const Color fadila = Color(0xFF22C55E);
  static const Color permissible = Color(0xFFF59E0B);
  static const Color makruh = Color(0xFFEF4444);
  static const Color missed = Color(0xFF9CA3AF);

  // ─── Фоны (Light) ───
  static const Color background = Color(0xFFF5F5FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceSecondary = Color(0xFFEEEEF4);
  static const Color surface3Light = Color(0xFFE4E4EC);

  // ─── Фоны (Dark) ───
  static const Color backgroundDark = Color(0xFF0A0A0C);
  static const Color surfaceDark = Color(0xFF141418);
  static const Color surfaceSecondaryDark = Color(0xFF1C1C22);
  static const Color surface3Dark = Color(0xFF24242C);

  // ─── Frosted / Glass ───
  static const Color frostedLight = Color(0xE6F9F9FB);
  static const Color frostedDark = Color(0xEB16161E);

  // ─── Текст (Light) ───
  static const Color textPrimary = Color(0xFF111118);
  static const Color textSecondary = Color(0xFF6B6B80);
  static const Color textTertiary = Color(0xFFA0A0B2);

  // ─── Текст (Dark) ───
  static const Color textPrimaryDark = Color(0xFFF0F0F5);
  static const Color textSecondaryDark = Color(0xFF8888A0);
  static const Color textTertiaryDark = Color(0xFF55556A);

  // ─── Разделители ───
  static const Color separator = Color(0xFFE0E0EA);
  static const Color separatorDark = Color(0x0FFFFFFF);

  // ─── Навбар (матовое стекло) ───
  static const Color navBarLight = Color(0xE6F9F9FB);
  static const Color navBarDark = Color(0xEB16161E);

  // ─── Утилиты ───
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color ringTrack = Color(0xFFE5E5ED);
  static const Color ringTrackDark = Color(0xFF24242C);

  // ─── Hero карточка ───
  static const Color heroStartLight = Color(0xFFFFFFFF);
  static const Color heroEndLight = Color(0xFFF0F0FA);
  static const Color heroStartDark = Color(0xFF13131A);
  static const Color heroEndDark = Color(0xFF181822);

  // ─── Радиусы ───
  static const double radiusS = 16.0;
  static const double radiusM = 22.0;
  static const double radiusL = 28.0;
  static const double radiusXL = 36.0;
}